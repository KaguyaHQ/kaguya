#!/bin/sh

# Single-node distribution on loopback so `bin/kaguya remote` / `rpc` works
# from inside the container. Not reachable from outside the container.
export RELEASE_DISTRIBUTION="name"
export RELEASE_NODE="kaguya@127.0.0.1"

# Uncomment to send crash dumps to stderr
# This can be useful for debugging, but may log sensitive information
# export ERL_CRASH_DUMP=/dev/stderr
# export ERL_CRASH_DUMP_BYTES=4096
